/** Single source of truth for pricing-card CTA buttons.
 *  "Get Free Report" (free tier) and both "Book a Slot" buttons must render identically. */
export const pricingCta = {
  variant: 'primary',
  size: 'lg',
  className: 'w-full',
} as const;